from setuptools import setup, find_packages

setup(
    name="semanthic_book_recommender",
    version="1.0.0",
    author="Ajibade Boluwatife",
    description="A semantic book recommender powered by NLP and machine learning.",
    packages=find_packages(),
    install_requires=[
        # Add dependencies listed in requirements.txt
    ],
    python_requires='>=3.8',
)
